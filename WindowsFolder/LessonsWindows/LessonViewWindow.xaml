<Window x:Class="TuteefyWPF.WindowsFolder.LessonsWindows.LessonViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="View Lesson"
        Width="850"
        SizeToContent="Height"
        MaxHeight="{x:Static SystemParameters.MaximizedPrimaryScreenHeight}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <DropShadowEffect x:Key="ShadowEffect"
                          Color="#888"
                          BlurRadius="10"
                          ShadowDepth="2"
                          Opacity="0.4"/>
    </Window.Resources>

    <!-- Main Card -->
    <Border Width="Auto"
            Effect="{StaticResource ShadowEffect}"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            CornerRadius="20">

        <!-- Gradient background -->
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="White" Offset="0"/>
                <GradientStop Color="#f9f6ff" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <StackPanel Width="810" Height="Auto">
            <!-- Header Bar -->
            <Border Padding="14">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#9600ff" Offset="0"/>
                        <GradientStop Color="#AEBAF8" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <TextBlock Text="View Lesson"
                           Foreground="White"
                           FontSize="20"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>
            </Border>

            <!-- Content area (read-only, styled like AddLessonsWindow) -->
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          MaxHeight="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=MaxHeight}">
                <Grid Margin="20,20,20,20" Height="Auto" Width="Auto">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Lesson Title -->
                    <StackPanel Margin="10" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
                        <TextBlock Text="Lesson Title"
                                   FontWeight="Bold"
                                   FontSize="13"
                                   Foreground="#5E35B1"/>
                        <Border CornerRadius="6"
                                BorderBrush="#C7B8FF"
                                BorderThickness="1"
                                Background="White">
                            <TextBox x:Name="TitleText"
                                     IsReadOnly="True"
                                     Height="41"
                                     FontSize="13"
                                     Padding="5"
                                     Background="Transparent"
                                     BorderThickness="0"/>
                        </Border>
                    </StackPanel>

                    <!-- Lesson Code -->
                    <StackPanel Margin="10" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                        <TextBlock Text="Lesson Code"
                                   FontWeight="Bold"
                                   FontSize="13"
                                   Foreground="#5E35B1"/>
                        <Border CornerRadius="6"
                                BorderBrush="#C7B8FF"
                                BorderThickness="1"
                                Background="White">
                            <TextBox x:Name="CodeText"
                                     IsReadOnly="True"
                                     Height="41"
                                     FontSize="13"
                                     Padding="5"
                                     Background="Transparent"
                                     BorderThickness="0"/>
                        </Border>
                    </StackPanel>

                    <!-- Content -->
                    <StackPanel Margin="10" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
                        <TextBlock Text="Lesson Content"
                                   FontWeight="Bold"
                                   FontSize="13"
                                   Foreground="#5E35B1"/>
                        <Border CornerRadius="6"
                                BorderBrush="#C7B8FF"
                                BorderThickness="1"
                                Background="White">
                            <TextBox x:Name="ContentText"
                                     IsReadOnly="True"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     Height="240"
                                     Margin="0,5,0,0"
                                     FontSize="13"
                                     Padding="5"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     VerticalScrollBarVisibility="Auto"/>
                        </Border>
                    </StackPanel>

                    <!-- File link / info -->
                    <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" VerticalAlignment="Center">
                        <TextBlock Text="Attached File:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#5E35B1"/>
                        <TextBlock x:Name="FileSection" VerticalAlignment="Center" TextWrapping="NoWrap"/>
                    </StackPanel>

                    <!-- Action Buttons -->
                    <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="10,20,10,10">
                        <Button x:Name="OpenFileButton" Content="Open File" Width="120" Height="38" Margin="0,0,8,0" Click="OpenFileButton_Click" Visibility="Collapsed" Style="{StaticResource AnimatedGradientButton}"/>
                        <Button x:Name="DownloadButton" Content="Download" Width="120" Height="38" Margin="0,0,8,0" Click="DownloadButton_Click" Visibility="Collapsed" Style="{StaticResource AnimatedGradientButton}"/>
                        <Button Content="Close" Width="120" Height="38" Click="Close_Click" Style="{StaticResource AnimatedGradientButton}"/>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </StackPanel>
    </Border>
</Window>