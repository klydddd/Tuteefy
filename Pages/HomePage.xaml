<Page
    x:Class="TuteefyWPF.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TuteefyWPF.UserControls"
    mc:Ignorable="d"
    Title="HomePage">

    <Page.Resources>
        <DropShadowEffect x:Key="ShadowEffect" Color="#888" BlurRadius="10" ShadowDepth="2" Opacity="0.4"/>
    </Page.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Hidden" 
                      HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        CornerRadius="20"
                        Padding="20"
                        Effect="{DynamicResource ShadowEffect}"
                        MinHeight="350">
                    <Border.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="White" Offset="0"/>
                            <GradientStop Color="#f9f6ff" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <DockPanel>
                        <TextBlock Text="Task Calendar" FontSize="20" FontWeight="Bold" Foreground="#5E35B1" DockPanel.Dock="Top" Margin="0,0,0,15"/>
                        <local:Calendar Loaded="Calendar_Loaded" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                    </DockPanel>
                </Border>

                <Border Grid.Row="1"
                        Margin="0,20,0,0"
                        CornerRadius="20"
                        Padding="20"
                        Effect="{DynamicResource ShadowEffect}"
                        MinHeight="250">
                    <Border.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="White" Offset="0"/>
                            <GradientStop Color="#f2f0ff" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <DockPanel>
                        <TextBlock Text="Student Scores (Per Quiz)"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#5E35B1"
                                   DockPanel.Dock="Top"
                                   Margin="0,0,0,15"/>

                        <lvc:CartesianChart DockPanel.Dock="Top"
                                            MinHeight="150"
                                            Margin="0,5,0,0"
                                            Series="{Binding SeriesCollection}" 
                                            LegendLocation="Right">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Quizzes" Labels="{Binding Labels}">
                                    <lvc:Axis.Separator>
                                        <lvc:Separator Step="1"/>
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>

                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Score" MinValue="0"/>
                            </lvc:CartesianChart.AxisY>

                        </lvc:CartesianChart>
                    </DockPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>